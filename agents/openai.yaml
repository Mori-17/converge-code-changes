interface:
  display_name: "代码变更收敛"
  short_description: "在修复和反复修改中消除旧分支、兜底与过程叙述，收敛成唯一的当前实现"
  default_prompt: "Use $converge-code-changes to revise this implementation into one clean current-state solution."

policy:
  allow_implicit_invocation: true
