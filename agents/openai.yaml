interface:
  display_name: "代码变更收敛"
  short_description: "点名删除内容时清除全部实例、相关残留与同义替代，并收敛最终实现"
  default_prompt: "Use $converge-code-changes to remove every explicitly named target and its owned residue without paraphrasing or replacing it."

policy:
  allow_implicit_invocation: true
